# Orchestrator rulings, 2026-09-03 (second owner round + adjudications) — binding for every agent below
Owner answers (2026-09-03, second round): phone video = transport (a) OR (b), "lowest possible latency and general comfort is priority" → ORCHESTRATOR ADJUDICATION OD-16: transport (a) WHEP/WebRTC pulled from the laptop's mediamtx into a bundled WKWebView; the iPhone_rc rule "one outbound send site" is amended to "one outbound INTENT send site; a read-only media pull is a viewer consumer". Latency is measured glass-to-glass at slice 2 on the simulator/bench and recorded `[bench-TBD]`; (b) native RTSP+VideoToolbox stays the fallback ONLY if the measured latency is unacceptable to the owner. Design details Q3–Q8 accepted as recommended (DRIVE profile; auto-start when the stream is live; reuse the existing Windows-host setting, no auto-derive; one clarifying sentence in the canonical contract; scoped GS change so mediamtx is reachable from the hotspot interface).
OD-17 (head tracking): dtClampMs = 50 ms is RATIFIED by the owner as a policy constant (D10 spirit); R12's signed-constants scope is WIDENED to the full calib.go schema (every field the record refuses to load without — incl. blend_start_deg, blend_full_deg, fault_recovery_valid_ms, lowpass_tau_ms, invalid threshold/window). FIRST_ACTIVE stays NO-GO.
OD-18 (R15 transport): the gated mapper build reaches the Windows VM as a `git bundle` / archive copied in by hand — NEVER a push; the hook stays intact; the bundle is deleted after the session; recorded in the plan's R15 row. R15 runs with no serial port and no TX (it is the arbiter's device-loss disarm, observed via diagnostics).
Adjudications of builder questions (orchestrator, within A8 authority):
- iPhone fix branch: (1) speed values outside [0, 1000] km/h render the placeholder `--` (OD-10 style), not "1000 km/h"; in-band values render as-is. (2) The drive HUD's "TELEMETRY STALE >1S" / "TELEMETRY DATA LOST >3S" strings become plain language ("signal is a moment behind" / "signal lost — the car is stopping itself"); the contract state names stay visible only in Debug. (3) Demo mode session-only and off at cold start: ACCEPTED. (4) Banner wording accepted for now (owner editorial pass later, booklet voice).
- GS branch A: pin the extracted binary (confirmed); windows_amd64 digest is recorded by the orchestrator after the first windows-latest run, then `--require-pin` goes into CI; retry-every-2-s on mediamtx 'error' ACCEPTED; one `.bak` generation ACCEPTED; mirror sentence lands with the GS re-mirror after the iPhone branch merges.
- control-fw fix branch: Hall guard re-arms every 1 s (duty-cycle limit, not a latch): ACCEPTED as ruled.
- soundlight fix branch: loop WDT at the framework's 5 s (not board #1's 2 s) ACCEPTED with the asymmetry documented at the call site; low-battery layer move ACCEPTED as the safe direction; 6/255 floor is `[bench-TBD]`.
- mapper branch A: exit-1 on an unfilled profile ACCEPTED (GS branch B must surface the exit as a plain-language race-day failure, never a silent 'running'); the `make(chan os.Signal)` vet finding goes to mapper branch B; multi-transmitter configs print a line and start nothing: ACCEPTED.
- B4 (Windows validation): scripts REQUIRE PowerShell 7 (`#Requires -Version 7.0`), `Resolve-W17Shell` fails loudly instead of falling back, the VM runbook adds `winget install Microsoft.PowerShell` to guest setup; script 60 is reframed as HID-transition + mapper-process continuity, states R15 stays NO-GO and is discharged by nothing in the suite; the suite's "no serial port is ever opened" claim stays true by construction.
