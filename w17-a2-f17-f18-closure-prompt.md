# Session prompt — close F17 and F18 (and whatever they generate)

Paste into a Claude Code session started at `~/Documents/projects`. **No hardware.**

> **Opus 5 at `xhigh`.** Both findings are already characterised with mechanism and sections named —
> implementation, not discovery. Fable is not needed.

> ⚠ **CONCURRENCY.** `w17-control-fw`'s tree has been held by other sessions. Run `git worktree list`
> and `git branch --show-current` in both repos **first**; if a tree is on someone else's branch, do
> not `git checkout` — make your own worktree outside `~/Documents/projects`. See `CLAUDE.md` →
> *Concurrent sessions*. Re-check HEAD immediately before committing.

---

Close **F17** and **F18** from
`w17-control-fw/project-review/14_a2_staged_gates_adversarial_review.md`. Both were generated by the
F15/F16 closure and recorded rather than fixed — correctly. That register is authoritative; read the
full write-ups, not just the summary below.

⚠ **Branch off `main` in both repos.** (Corrected 2026-08-04: an earlier version of this line said to
work on `docs/a2-revision-pass`. That was true when written and became wrong at consolidation — the
workspace branch is merged and deleted, and control-fw's is **fully contained in `main`**, which is
now *ahead* of it at `d295f70`. Branching off the old ref would fork from a stale base and buy you a
merge for nothing. Verify with `git log --oneline main..docs/a2-revision-pass` — empty means
contained.)

**Note on control-fw's working tree:** it may still be *checked out* on that stale merged branch from
the F15/F16 session. That is not an invitation to use it. Follow the concurrency rule — if the tree
is on a branch that is not yours, work in your own worktree off `main`.

## F17 — the enlarged set was never walked

§3 rule 2's exceptions list legitimises 13/14 → GND and stops there. **S1r is the signal↔signal
matrix**, and with a pull-down on 13 and one on 14 both returning to the star node, the pair reads
**through them in series** — roughly 2× the fitted value. Beeper mode is safe; **resistance mode is
not**, and that variant becomes the norm exactly when F12's caliper forces hard-wired boards. No
pull-down value is mandated anywhere, so two ordinary 4.7 kΩ parts put 13↔14 at ≈9.4 kΩ: **a rule-2
FAIL on a correct board**, reported to the builder as "the ESC signal line and the steering line are
bridged."

The F15/F16 pass already did the structural half — the exceptions list is now **closed by
construction**, with a standing instruction that any revision adding a deliberate resistance extends
it in the same edit. **Your job is the remaining concrete half:** make S1r's expectation correct in
both meter modes when PD1 is populated.

## F18 — an unsequenced fit moment that decides a measurement

PD1's pull-downs have **no stated location and no fit moment**, and the guide never mentions them at
all. It bites through rule 2: a **harness-side** pull-down survives unseating, a **board-side** one
does not — so **F15's own exception is only measurable under one of them**, and nothing picked.

The F15/F16 pass decided it on the document's own precedent (F11's Hall pull-up, F13.3's C3):
**harness side, at the ESP32 #1 socket positions, fitted at S4**, with the honest A2-time state
recorded as **NOT POPULATED** (R04's evidence is B1.4 scope, i.e. Phase B). **Implement that** across
the checklist and the guide, which currently does not mention PD1 anywhere.

Note the coupling: **F18's answer is what makes F17's fix well-defined.** Do F18 first.

## Also carried over, verify not fixed

The **photo item 14** point recorded in F16's closure row: C1 was the only PDB electrolytic with no
frame, while §12 Part 1 claims cap-stripe polarity is photo-checkable and **no no-power reading
distinguishes a reversed 1000 µF from a correct one** (M3 reads a charging cap either way). Confirm
it landed, or land it.

And re-confirm the **two owed measurements are still owed** — the socket-stack caliper and the MH-ET
adjacency list. Neither may be quietly promoted; both are bench work.

## The standing instruction

**If closing these generates an F19, record it — do not quietly fix it.** The register is the point,
and the sequence F15→F17, F16→F18 is now the third and fourth time a fix inside this register has
produced the next finding. Walk your own closures the way the F15/F16 pass did: for each, name the
set your fix enlarges or the moment it makes load-bearing, and check the rows that consume it. That
pass found F17 and F18 by doing exactly this, and said honestly that a walk is not a proof.

## Output

Updated checklist and guide, closure rows in the register in the same finding → what changed → where
format, and then the one sentence the bench is waiting on: **is the checklist still executable, and
did anything here change the caliper-first precondition?**

**Safety:** documents only. Nothing built, powered, flashed, or connected. Closing findings makes A2
*executable*, not *executed* — **A2 stays NOT-EXECUTED, Phase B stays BLOCKED**. Show diffs before
committing.
