# Close-out baseline — 2026-09-04 (part A, READ-ONLY)

Runner: Sonnet 5. Every checkout confirmed `git status` clean and byte-identical
before and after this run (no commits, checkouts, resets, or pushes made; nothing
flashed/powered; no serial ports opened). Pass/fail below is decided from **exit
codes**, not grep-filtered output; each exit code was captured directly off the
command that produced it (not through a `tee`/pipe that would mask it).

## Summary table

| repo | HEAD (short) | matches state file? | tests | other checks | CI conclusion at HEAD |
|---|---|---|---|---|---|
| w17-control-fw | `39a4f3c` (main) | yes | `pio test -e native` exit 0 — **360/360** passed | 5 envs build exit 0 (sizes below); `delivery_shape_check.sh --strict` exit 0; `test_registration_check.sh` exit 0 (13 suites, 360 registered, no orphans) + `--print-total` → 360; `link2_copy_check.sh --strict --sibling` exit 0 (code tier: 4/4 shared files identical; doc tier: REPORT-only, hand-diffed, no normative drift — see note) | run 33814317181, headSha 39a4f3c**...**, **success** (both jobs `link2-drift` and `build-and-test` = success) |
| w17-soundlight-fw | `7220c08` (main) | yes | `pio test -e native` exit 0 — **150/150** passed | esp32dev + esp32dev_sim build exit 0 (sizes below); `delivery_shape_check.sh` exit 0 (positive control confirms the scanner works, esp32dev clean) | run 33814343711, headSha 7220c08**...**, **success** |
| w17-ground-station | `263e69a` (main) | yes | `npx vitest run` exit 0 — **1685/1685 passed, 73/73 files** | `npm run contract:check` exit 0 (in sync, canonical @ iPhone 7aaf2cf — matches iPhone HEAD below); `W17_MAPPER_REPO=.../w17-mapper npm run proto:check` exit 0 (head-intent OK, read-only streams OK); electron-builder **not run** (per brief — not exercised on this Mac; no `--dir` packaging check performed) | run 33818863341, headSha 263e69a**...**, **success** |
| iPhone_rc | `7aaf2cf` (main) | yes | `./scripts/dev_check.sh` exit 0 — xcodebuild test suite **156/156** passed (0 failures) | `check_send_sites.py` ok (27 Swift sources, 27 app-target, +1 bundled page script); all fixture/example/script self-checks in the log report `ok` | run 33816071199, headSha 7aaf2cf**...**, **success** |
| w17-mapper | `6e99d51` (branch `w17-headtrack`, **NOT main**) | yes — **pre-mapper-B** (HEAD is exactly `6e99d51`, the marker value named in the brief) | `go test ./pkg/...` exit 0 — **240 pass** (193 top-level + 47 subtests, 0 fail, 0 skip, matches RUN count); `go test -race ./pkg/...` exit 0 — **-race clean**, same packages | four `.githooks/pre-push` greps run by hand at HEAD: all four `git grep` invocations exited 1 (no match) — i.e. none of the forbidden patterns (`w17_first_active` tag, `FIRST_ACTIVE` identifier, non-terminal `HEAD_INTENT_STATE_ACTIVE`, any-case `first_active`) are present, so the hook would **allow** a push at this HEAD | **no CI runs exist for this repo at all** (`gh run list` empty on both `w17-headtrack` and `main`, and unfiltered) — expected per state file: mapper has never had a CI run; its first-ever run is the release-workflow dispatch deferred until mapper B lands |
| w17-3d-codex | `5dddedb` (main) | yes | n/a (no unit tests) | `11_cad/render.sh --table` exit 0 — **rendered: 17  failed: 0** (matches state file exactly); full per-model triangle-count/bbox table captured, all `ok` | no `.github/workflows/` directory exists — **no CI configured** for this repo (not a gap: it has none to be red or green) |
| workspace (this repo) | `85f7c12` (main) | see note below | n/a | `scripts/check_readiness_runbook_links.sh --workspace-root /Users/vitaliykhomenko/Documents/projects` exit 0 — exactly **one** `MISSING-SCRATCH` line (`review-seeds/w17-mapper.v2report.json`, explicitly expected/non-resolvable) and **zero** other `MISSING` lines; several `UNVERIFIED` (ambiguous bare-filename) lines present but the script does not fail on those | no `.github/workflows/` directory exists — **no CI configured** for this repo |

## Sizes (five control-fw envs, all exit 0)

| env | RAM | Flash |
|---|---|---|
| esp32dev | 7.0% (22948/327680 B) | 23.2% (304013/1310720 B) |
| esp32dev_sim | 7.0% (22964/327680 B) | 23.4% (306385/1310720 B) |
| esp32dev_tuning | 7.1% (23156/327680 B) | 23.7% (310369/1310720 B) |
| esp32dev_btshowoff | 26.6% (87076/327680 B) | 23.6% (742305/3145728 B) |
| esp32dev_simbt | 7.0% (23036/327680 B) | 23.5% (307793/1310720 B) |

## Sizes (two soundlight envs, both exit 0)

| env | RAM | Flash |
|---|---|---|
| esp32dev | 7.2% (23560/327680 B) | 22.8% (299441/1310720 B) |
| esp32dev_sim | 7.2% (23592/327680 B) | 23.0% (301181/1310720 B) |

## Discrepancies vs W17_CURRENT_STATE.md §1

1. **None of the six product-repo trunk SHAs have moved.** control-fw `39a4f3c`,
   soundlight `7220c08`, ground-station `263e69a`, iPhone_rc `7aaf2cf`, mapper (branch
   `w17-headtrack`) `6e99d51`, 3d-codex `5dddedb` — all confirmed by `git rev-parse HEAD`
   to match the state file exactly, and all worktrees were clean before and after.
2. **mapper is still pre-mapper-B**, per the brief's own marker: HEAD is exactly
   `6e99d51`. A second worktree exists at
   `.../scratchpad/wt-mapper-fixB` on branch `fix/gamepad-hotplug-and-lint-teeth`
   (tip `e845dbd`, per the state file's own ledger) — that is someone else's in-flight
   work; it was left untouched and not read into this baseline (the brief scopes this
   run to the trunk HEAD, `w17-headtrack` @ `6e99d51`).
3. **The workspace repo itself has advanced past the commit implied by the git
   context snapshot at session start** (`0c40b6e` → now `85f7c12`), which is expected
   and by design: the state file explicitly says workspace HEAD is "see `git log -1`
   (this commit)" with no fixed pin, and `CLAUDE.md`'s concurrency section documents
   that this shared tree moves under any given session. `git log --oneline -10`
   confirms all ten most-recent commits are `docs(state):`/`docs(program,state):`
   entries recording other agents' landings (GS B, mapper B round-2 fixer, A12
   adjudications, etc.) — no non-docs change, nothing this session needs to react to.
4. **link2 doc-tier REPORT is real but non-normative** (control-fw ⇄ soundlight
   `docs/link2_protocol.md`): hand-diffed per the script's own instruction. The
   difference is entirely repo-local prose/framing (which repo "owns" vs "is a
   receiver", where the checker script lives, verified-BT_SOLO commentary present only
   in the control-fw copy). Frame layout, the 17-byte payload table, CRC handling, and
   the 500 ms staleness rule are unchanged in both copies — no normative drift found,
   nothing to re-sync.
5. **CI conclusions match the state file's recorded run IDs exactly**, including
   headSha, for all four repos that have GitHub Actions CI (control-fw, soundlight,
   ground-station, iPhone_rc) — re-confirmed live via
   `gh run list --branch <trunk> --limit 1 --json headSha,conclusion,databaseId`
   rather than trusted from the doc. control-fw's run was additionally checked at the
   job level (`gh run view <id> --json jobs`): both `link2-drift` and `build-and-test`
   show `success`, consistent with the state file's note about a since-resolved
   transient race on that job.
6. **w17-mapper, w17-3d-codex, and the workspace repo have no CI history to report**
   — mapper has GitHub Actions workflows defined but zero runs have ever executed
   (`gh run list` empty, matching the state file's statement that mapper's first-ever
   CI run is the still-pending release-workflow dispatch); 3d-codex and the workspace
   repo have no `.github/workflows/` directory at all, so "CI conclusion" is
   structurally not applicable for either — not a red/failing state, just none
   configured.
7. GS's `electron-builder` step was **not run**, per the brief's own carve-out ("if
   electron-builder runs on this Mac" / "note if not") — it was not attempted this
   session, so nothing is claimed about NSIS/packaging beyond what the state file
   already records from the Windows CI runner.

## File written

`/private/tmp/claude-501/-Users-vitaliykhomenko-Documents-projects/9a131be5-75e5-4b91-9d54-0fedf52c1bf8/scratchpad/closeout/baseline-2026-09-04.md`


## Mapper re-run after branch B landed (2026-09-04 08:33)

| repo | HEAD | tests | other checks | CI at HEAD |
|---|---|---|---|---|
| w17-mapper | ebf89fa (w17-headtrack, pushed through .githooks/pre-push) | `go test ./pkg/...` exit 0, 295 PASS lines / 0 FAIL; `go test -race ./pkg/...` exit 0, zero DATA RACE | the full pre-push hook driven with a ref line on stdin against the trunk tip: exit 0 (all four checks clean) | release workflow `w17-windows-release.yaml` — triggered by the push of ebf89fa (it runs on every push to w17-headtrack; no manual dispatch needed): run 33840857908 **RED** at 'Build + test binary' — build OK, all packages OK except pkg/link: three Windows-only timing tests (15.6 ms timer granularity; log in closeout/mapper-release-run-33840857908-fail.log) → fix branch → re-run |
